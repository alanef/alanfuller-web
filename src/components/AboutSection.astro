---
const stats = [
  { value: '80,000+', label: 'Businesses use my products' },
  { value: 'UK', label: 'Based Expert' }
];
---

<section class="py-24 bg-white">
  <div class="container mx-auto px-6">
    <div class="max-w-5xl mx-auto">
      <div class="text-center mb-16">
        <h2 class="text-section-title text-gray-900 mb-6">
          Experience You Can Trust
        </h2>
        <p class="text-xl lg:text-2xl text-gray-700 leading-relaxed max-w-3xl mx-auto">
          I don't just build tools - I use them. Every solution comes from real problems I've solved, not theoretical ideas or trendy frameworks.
        </p>
      </div>
      
      <!-- Stats -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 max-w-2xl mx-auto">
        {stats.map((stat) => (
          <div class="text-center px-8 py-12 bg-gray-50 border border-gray-200">
            <div class="text-5xl md:text-6xl font-black text-[#1e3a8a] mb-3">
              {stat.value}
            </div>
            <div class="text-gray-700 font-semibold text-lg">
              {stat.label}
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>